{
  "name": "vercel-time-api-playground",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal Vercel API that returns the current time as JSON.",
  "scripts": {
    "test": "node scripts/test-now.js"
  }
}
